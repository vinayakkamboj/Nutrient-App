--- a/lib/PromptOrchestrator.ts
+++ b/lib/PromptOrchestrator.ts
@@ -9,6 +9,7 @@ export interface PromptOrchestratorInput {
 export interface PromptOrchestratorOutput {
   user_prompt: string;
   enhanced_prompt: string;
   provider: "anthropic" | "openai";
   model_requested: string;
   model_used: string;
@@ -16,6 +17,7 @@ export interface PromptOrchestratorOutput {
   response_html: string;
   errors: string[];
+  warnings?: string[];
   available_models?: string[];
 }

@@ -44,10 +46,49 @@ async function getAnthropicModels(apiKey: string): Promise<string[]> {
 }

 function selectBestModel(models: string[]): string | null {
   if (models.length === 0) return null;
-  const sonnet = models.find(m => m.toLowerCase().includes("sonnet"));
-  if (sonnet) return sonnet;
-  const opus = models.find(m => m.toLowerCase().includes("opus"));
-  if (opus) return opus;
+
+  // Helper to extract date suffix (YYYYMMDD) from model name
+  const extractDate = (modelName: string): number => {
+    const match = modelName.match(/(\d{8})$/);
+    return match ? parseInt(match[1], 10) : 0;
+  };
+
+  // Prefer Sonnet models, choose newest by date
+  const sonnetModels = models.filter(m => m.toLowerCase().includes("sonnet"));
+  if (sonnetModels.length > 0) {
+    if (sonnetModels.length === 1) return sonnetModels[0];
+
+    // Sort by date descending, pick newest
+    sonnetModels.sort((a, b) => extractDate(b) - extractDate(a));
+    return sonnetModels[0];
+  }
+
+  // Prefer Opus models, choose newest by date
+  const opusModels = models.filter(m => m.toLowerCase().includes("opus"));
+  if (opusModels.length > 0) {
+    if (opusModels.length === 1) return opusModels[0];
+
+    // Sort by date descending, pick newest
+    opusModels.sort((a, b) => extractDate(b) - extractDate(a));
+    return opusModels[0];
+  }
+
   return models[0];
 }

@@ -57,6 +98,7 @@ export async function PromptOrchestrator(
 ): Promise<PromptOrchestratorOutput> {
   const { user_prompt } = input;
   const errors: string[] = [];
+  const warnings: string[] = [];
   const provider = (process.env.LLM_PROVIDER?.toLowerCase() === "openai" ? "openai" : "anthropic") as "anthropic" | "openai";

   try {
@@ -71,7 +113,7 @@ export async function PromptOrchestrator(
       const apiKey = process.env.ANTHROPIC_API_KEY;
       if (!apiKey) {
         errors.push("ANTHROPIC_API_KEY not found in environment variables");
-        return buildErrorResponse(user_prompt, enhanced_prompt, provider, "", "", errors, []);
+        return buildErrorResponse(user_prompt, enhanced_prompt, provider, "", "", errors, warnings, []);
       }

       const anthropic = new Anthropic({ apiKey });
-      modelRequested = process.env.LLM_MODEL || "claude-3-sonnet-20240229";
+      modelRequested = process.env.LLM_MODEL || "claude-sonnet-4-5-20250929";
       modelUsed = modelRequested;

       try {
@@ -101,7 +143,7 @@ export async function PromptOrchestrator(
         const requestId = firstError?.requestID || "";

         if (isModelNotFound) {
-          errors.push(`Model "${modelRequested}" not found (request_id: ${requestId})`);
           availableModels = await getAnthropicModels(apiKey);

           if (availableModels.length > 0) {
             const fallbackModel = selectBestModel(availableModels);
             if (fallbackModel) {
-              errors.push(`Retrying with fallback: ${fallbackModel}`);
               modelUsed = fallbackModel;

               try {
@@ -121,9 +163,8 @@ export async function PromptOrchestrator(
                 if (content.type === "text") {
                   response_text = content.text;
                   response_html = extractAndValidateHTML(content.text);
-                  errors.length = 0;
-                  errors.push(`Success with fallback: ${fallbackModel}`);
+                  warnings.push(`Model "${modelRequested}" not found; used fallback "${fallbackModel}"`);
                 }
               } catch (retryError: any) {
                 const retryMsg = retryError instanceof Error ? retryError.message : String(retryError);
                 const retryRequestId = retryError?.requestID || "";
-                errors.push(`Fallback failed: ${retryMsg} (request_id: ${retryRequestId})`);
+                errors.push(`Model "${modelRequested}" not found (request_id: ${requestId}). Fallback to "${fallbackModel}" failed: ${retryMsg} (request_id: ${retryRequestId})`);
                 response_text = `Error: ${retryMsg}`;
                 response_html = createErrorHTML(retryMsg, provider, modelRequested, modelUsed, availableModels, retryRequestId);
               }
             } else {
-              errors.push("No suitable fallback model found");
+              errors.push(`Model "${modelRequested}" not found (request_id: ${requestId}). No suitable fallback model found`);
               response_text = "Error: No suitable fallback";
               response_html = createErrorHTML("No suitable fallback", provider, modelRequested, modelUsed, availableModels, requestId);
             }
           } else {
-            errors.push("Failed to fetch available models");
+            errors.push(`Model "${modelRequested}" not found (request_id: ${requestId}). Failed to fetch available models`);
             response_text = `Error: ${firstError.message}`;
             response_html = createErrorHTML(firstError.message, provider, modelRequested, modelUsed, [], requestId);
           }
@@ -159,6 +200,7 @@ export async function PromptOrchestrator(
         response_text,
         response_html,
         errors,
+        warnings,
         available_models: availableModels.length > 0 ? availableModels.slice(0, 10) : undefined,
       };
     } else {
@@ -165,7 +207,7 @@ export async function PromptOrchestrator(
       const apiKey = process.env.OPENAI_API_KEY;
       if (!apiKey) {
         errors.push("OPENAI_API_KEY not found in environment variables");
-        return buildErrorResponse(user_prompt, enhanced_prompt, provider, "", "", errors, []);
+        return buildErrorResponse(user_prompt, enhanced_prompt, provider, "", "", errors, warnings, []);
       }

       const openai = new OpenAI({ apiKey });
@@ -196,6 +238,7 @@ export async function PromptOrchestrator(
         response_text,
         response_html,
         errors,
+        warnings,
       };
     }
   } catch (error) {
@@ -204,7 +247,7 @@ export async function PromptOrchestrator(
     errors.push(errorMsg);
     const { enhanced_prompt } = prompt_manager({ user_prompt });
-    return buildErrorResponse(user_prompt, enhanced_prompt, provider, "", "", errors, []);
+    return buildErrorResponse(user_prompt, enhanced_prompt, provider, "", "", errors, warnings, []);
   }
 }

@@ -313,6 +356,7 @@ function buildErrorResponse(
   modelRequested: string,
   modelUsed: string,
   errors: string[],
+  warnings: string[],
   availableModels: string[]
 ): PromptOrchestratorOutput {
   return {
@@ -325,6 +369,7 @@ function buildErrorResponse(
     response_text: `Error: ${errors.join("; ")}`,
     response_html: createErrorHTML(errors.join("; "), provider, modelRequested, modelUsed, availableModels, ""),
     errors,
+    warnings,
     available_models: availableModels.length > 0 ? availableModels.slice(0, 10) : undefined,
   };
 }
--- a/app/mcp/route.ts
+++ b/app/mcp/route.ts
@@ -299,9 +299,16 @@ const handler = createMcpHandler(async (server: any) => {
                     if (availableModels.length > 0) {
                       metaText += '\\n\\nAvailable Models:\\n' + availableModels.join('\\n');
                     }

                     metaContent.textContent = metaText;

+                    // Display warnings if any
+                    const warnings = toolOutput.warnings || [];
+                    if (warnings.length > 0) {
+                      const warningDiv = '<div style="padding: 12px 16px; background: #f59e0b; color: #fff; font-size: 13px;">⚠️ Warnings: ' + warnings.join('; ') + '</div>';
+                      errorContainer.innerHTML = warningDiv + errorContainer.innerHTML;
+                    }
+
                     // Display errors if any
                     if (errors.length > 0) {
-                      errorContainer.innerHTML = '<div class="error-display">⚠️ Errors: ' + errors.join('; ') + '</div>';
+                      const errorDiv = '<div class="error-display">⚠️ Errors: ' + errors.join('; ') + '</div>';
+                      errorContainer.innerHTML += errorDiv;
                     } else {
-                      errorContainer.innerHTML = '';
+                      if (warnings.length === 0) {
+                        errorContainer.innerHTML = '';
+                      }
                     }

                     // Initialize viewer with response_html
@@ -1570,7 +1577,9 @@ const handler = createMcpHandler(async (server: any) => {
         return {
           content: [
             {
               type: "text",
-              text: `✅ Processed request using ${result.provider} (${result.model_used})\n\n📝 User prompt: "${user_prompt}"\n\n${result.errors.length > 0 ? "⚠️ Errors: " + result.errors.join("; ") : "✨ Response generated successfully"}`,
+              text: `✅ Processed request using ${result.provider} (${result.model_used})\n\n📝 User prompt: "${user_prompt}"\n\n${
+                result.errors.length > 0 ? "⚠️ Errors: " + result.errors.join("; ") + "\n\n" : ""
+              }${result.warnings && result.warnings.length > 0 ? "⚠️ Warnings: " + result.warnings.join("; ") + "\n\n" : ""}${result.errors.length === 0 && (!result.warnings || result.warnings.length === 0) ? "✨ Response generated successfully" : ""}`.trim(),
             },
           ],
           structuredContent: {
@@ -1583,6 +1592,7 @@ const handler = createMcpHandler(async (server: any) => {
               response_text: result.response_text,
               response_html: result.response_html,
               errors: result.errors,
+              warnings: result.warnings,
               available_models: result.available_models,
             },
             timestamp: new Date().toISOString(),
